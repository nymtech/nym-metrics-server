package metrics

import (
	. "github.com/onsi/ginkgo"
)

var _ = Describe("metrics.Service", func() {
	Describe("Adding a mixmetric", func() {

	})
	// Describe("Listing mixmetrics", func() {
	// 	Context("When no metrics exist yet", func() {
	// 		It("should return an empty slice", func() {
	// 			empty := make([]models.MixMetric, 0)
	// 			assert.Equal(GinkgoT(), empty, mixMetrics)
	// 		})
	// 	})
	// })
})
